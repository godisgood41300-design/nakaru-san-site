VERCEL SETTINGS

Use these exact settings:

Framework Preset: Other
Install Command: npm install
Build Command: leave blank
Output Directory: leave blank

If Vercel forces an Output Directory, type:

public

IMPORTANT:

When uploading or pushing this project, Vercel must see these files at the top level:

index.html
public
api
package.json
vercel.json

If Vercel only sees a folder named vercel-easy, open that folder and upload the contents inside it instead.
